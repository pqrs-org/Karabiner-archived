<?xml version="1.0"?>
<root>
  <!--
      | パスワード入力では、入力ソースが日本語でも自動的に英語入力に切り替わる。
      | （日本語の入力ソースが一時的に無効になり、英語の入力ソースが有効になる。）
      |
      | OS X 10.10ではバグにより、この際の入力ソース変更通知が来ないので、
      | パスワード入力ダイアログでは強制的に無効にする。
      | （アカウント名の入力でも無効になるが、区別する方法がない。）
      |
      | （OS X 10.11では修正されているので、いずれ無効にする。）
  -->
  <not>PASSWORD</not>
</root>
